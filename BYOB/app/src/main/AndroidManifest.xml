<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vaibhav.byob">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="android.support.v4.app.CoreComponentFactory"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:appComponentFactory">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OfferPropertyActivity"
            android:label="@string/title_activity_main_screen" />
        <activity android:name=".UserAccountSettings" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".ShortlistedProperties" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_API_Key" />

        <activity android:name=".OfferPropertyMatchesScreen" />
        <activity android:name=".UserPossibleMatches" />
        <activity android:name=".DetailedUserMatchesView" />
        <activity android:name=".SearchProperties" />
        <activity android:name=".SearchActivityFoundPropertiesListView" />

    </application>

</manifest>